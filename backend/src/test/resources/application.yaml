bank:
  host:
    baseurl: http://localhost:9090

spring:
  main:
    banner-mode: off
  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: true
    open-in-view: false

logging:
  level:
    org:
      springframework: ERROR
